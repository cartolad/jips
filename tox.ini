[tox]
env_list = lint, test

[testenv:lint]
runner = uv-venv-lock-runner
dependency_groups = dev
commands =
    ruff check .

[testenv:test]
runner = uv-venv-lock-runner
dependency_groups = dev
commands =
    pytest
